def test_temp():
    """A trivial smoke test to ensure pytest collects at least one test.

    This prevents CI from failing when the rest of the test suite has been
    removed temporarily.
    """
    assert True
